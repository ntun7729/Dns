# Infrastructure-only overrides. DNS and FRP operational settings are configured
# from the Technitium console and /_bridge/ respectively.
PORT=10000
TECHNITIUM_CONFIG_DIR=/data/technitium
DNS_BRIDGE_DATA_DIR=/data/bridge
