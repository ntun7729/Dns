serverAddr = "203.0.113.10"
serverPort = 7000

# Optional: uncomment only when FRPS uses token authentication.
# auth.method = "token"
# auth.token = "replace-with-render-secret"

[[proxies]]
name = "dns-over-tls"
type = "tcp"
localIP = "127.0.0.1"
localPort = 8853
remotePort = 853
